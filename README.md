# 🏝 AI 创意岛

面向 **9–12 岁**孩子的 AI 编程学习应用（本地自用）。孩子用积木编程做游戏和动画，一个 AI 创意伙伴全程陪伴：出点子、给提示、讲概念、点评作品。

> ⚠️ 本项目是**独立 git 仓库**（父仓库已 ignore），因为里面会存孩子的学习数据、AI 对话记录和 API key——**不要 push 到公开仓库**。

## 功能一览

| 模块 | 说明 |
|------|------|
| 🪄 点子优先入口 | 地图首页是「今天想创造什么？」——选点子直接开工，本领在做中学，而不是先上课再做事 |
| 🧭 发现之路（无锁） | 6+3 段引导式探险：顺序 → 循环 → 事件 → 条件 → **调试** → 综合发布会 + 拓展岛；全部随时可进，只标「建议下一站」 |
| 🏔 拓展岛课程 | 「坐标小侦探」（数学·位置与方向）、「信息安全小卫士」（信息隐私与安全）、「和 Python 打招呼」（代码课） |
| 🎓 交叉学院 | **编程 × 学科**跨学科主题学习（新课标 10% 课时要求）：静夜思动画剧场（语文·古诗）、万花筒与正多边形（数学·外角和）、小星星节奏师（音乐·C 大调音阶）、太阳系运转（科学·圆周运动）、幸运大转盘（数学·统计与概率） |
| 📐 数学岛 | **小学→初中→高中全学段数学**（标杆板块，对标 2022 版课标+高中衔接）：🏛 小学馆（积木）：数轴加减/负数、跳格子乘法、对折分数、行程实验室、极坐标寻宝、割圆术·祖冲之；🏛 初中馆（Python）：代数式、一次函数画图机、勾股定理实验室、抛物线之美；🏛 高中馆（Python）：正弦波发生器、指数爆炸·国王的麦粒。**核心设计：数学答案就是通关位置——算对了，角色才停得在那** |
| 🧠 AI 实验室 | **训练你自己的 AI**：摄像头拍样本 → 看它学会认手势/表情 → 带着「AI 大脑」去工作台用「当 AI 认出」积木做手势游戏（纯本机计算，画面不离开浏览器） |
| 🧩 积木工作台 | 24 块中文积木（含 AI 识别积木），拖拽即编程，死循环不会卡死 |
| 🎭 小剧场 | 画布舞台：角色、说话气泡、目标旗子、边缘反弹、音效（WebAudio 合成，零素材） |
| 🤖 AI 创意伙伴（主动引导） | 四模式：💡灵感 / 🆘提示 / 📖讲解 / 🌟点评 + 🎤 语音说话；**引导大脑**会在关键时刻主动开口：放第一块积木、第一次跑通、出错、发呆时——不说教，只递台阶 |
| ✨ 自由创造岛 | 全积木开放，随时保存作品 |
| 🖼 作品墙 | 作品缩略墙 + 放映模式（含键盘事件，游戏类作品可直接玩）+ ❤️ 家庭点赞 |
| 📗 课标对标 | 每课标注《义务教育信息科技课程标准（2022年版）》学段/模块/知识点；通关庆祝与家长端学情报告展示知识点覆盖 |
| 🏆 结业证书 | 走完发现之路即解锁，可打印/PDF 贴墙 |
| 🔥 创作之火 | 连续学习天数（温和版激励，不搞强制打卡） |
| 🛡 家长中心 | PIN 进入：学习进度、**学情报告（课标知识点覆盖）**、**全部 AI 对话记录**、伙伴人设、提示严格度、每日时长提醒 |

**引导式设计（而非课堂式）**：没有"目标/任务清单"的说教面板——左侧是**探险日志**，校验器在暗中工作，孩子做对了"发现"自动点亮 ✨；通关叫"新本领 GET"；课程全部开放不上锁，只给"💡建议下一站"。
**为孩子设计的创作细节**：↩️↪️🧹撤销/重做/整理、🐢三档速度、💾 双草稿（积木+代码）自动保存、👀 魔法代码预览、🐍 真 Python 代码模式（可直接写、可从积木生成）、发现点亮时伙伴喝彩（夸努力不夸聪明）、20-20-20 护眼远眺、可选到时锁定。

**家长端学情报告**：课标知识点覆盖矩阵 + **跨学科覆盖**（编程×学科项目与知识点）+ 学习投入统计，可打印存 PDF。
**隐私架构**：所有数据（进度、作品、对话、训练场样本、摄像头画面）仅在本机，不上云；对话全量落盘家长可查。

## 快速开始

需要 Node.js 18+（开发用 24 验证通过）。

```bat
:: 双击即可（首次自动安装依赖、构建、生成配置）
start.bat
```

然后浏览器打开 **http://127.0.0.1:8787**

### 启用真正的 AI 伙伴（重要）

不配置也能完整游玩（AI 伙伴以离线替身模式回复）。要接上真模型：

1. 用记事本打开 `data/config.json`
2. 填入任意 OpenAI 兼容接口的 `apiKey`（默认模板是智谱 GLM；DeepSeek 把 `baseURL` 换成 `https://api.deepseek.com`、`model` 换成 `deepseek-chat`）
3. 重启 `start.bat`

key 只存在本机 `data/config.json`（已被 gitignore），不会入库。

### 让平板在同一 WiFi 玩

`data/config.json` 里把 `server.host` 改成 `"0.0.0.0"`，重启后用电脑的局域网 IP 访问（如 `http://192.168.1.5:8787`）。查 IP：`ipconfig` 看 IPv4 地址。

### 开发模式

```bat
start-dev.bat   :: server(8787, 热重载) + vite(5173, 热更新)
```

测试：`npm test`（关卡校验引擎 + 儿童安全过滤的单元测试）。

## 目录结构

```
creative-island/
├── apps/
│   ├── web/            # Vite + React + TS 前端（Blockly 积木、舞台、AI 伙伴 UI）
│   └── server/         # Express 后端（API、LLM 网关、安全过滤、JSON 存储）
├── shared/types.ts     # 前后端共享类型
├── content/lessons/    # 课程内容 JSON（改这里=改课程，改完刷新页面即生效）
├── data/               # 孩子数据与私密配置（gitignore，永不入库）
│   ├── config.json     # API key、端口、家长 PIN（默认 1234，请修改）
│   ├── profiles.json   # 角色档案
│   ├── progress/       # 学习进度与每日用时
│   ├── projects/       # 保存的作品
│   └── chatlogs/       # AI 对话全量记录（家长面板可查）
├── start.bat           # 一键启动
└── start-dev.bat       # 开发模式
```

## 怎么加一节课（不用写代码）

复制 `content/lessons/basics-01.json` 改一改：

- `order`：决定地图上的顺序（第 6 课就写 6）
- `toolbox`：这节课允许哪些积木（积木类型见 `apps/web/src/blocks/definitions.ts`）
- `actor` / `targets`：角色初始位置和目标点
- `tasks[].check`：自动判题规则，支持：
  - `{"type":"block_used","block":"island_repeat"}` — 用了某积木
  - `{"type":"block_used_any","blocks":["island_costume","island_change_size"]}` — 用了其中任一积木
  - `{"type":"block_count_min","block":"island_wait","count":2}` — 某积木至少 N 块
  - `{"type":"block_count_total_min","count":8}` — 总积木数至少 N
  - `{"type":"say_text"}` — 角色说过话
  - `{"type":"actor_reach","targetIndex":0}` — 到达第 N 个目标点
  - `{"type":"manual"}` — 孩子自己打勾
- `tasks[].optional: true` — ⭐选做挑战任务，不计入通关条件
- `starterXml` — 预先摆好的积木（修复挑战课用它给孩子一段"生病的程序"）
- `tasks[].hintPrompts`：AI 给提示时的参考要点（从方向到差一步，写 2–3 条）
- `subject` — 交叉学科标注：`{name: "数学·数与代数", emoji: "🔢", points: [...]}`，展示在地图学科徽标、任务面板与家长端跨学科报告
- `codeLesson: true` + `starterCode` — Python 代码课（初中馆/高中馆/和 Python 打招呼）
- `curriculum` — 课标对标标注：`{stage: "第三学段（5-6年级）", module: "身边的算法", points: ["顺序结构", ...]}`，展示在任务面板、通关庆祝与家长端学情报告

保存后刷新页面即生效。

## 儿童安全设计

- AI 对话被限制在编程创作话题内；出话题会被温柔拉回
- 孩子输入含手机号/住址等个人信息时**不转发给大模型**，直接提示保护隐私
- 模型输出自动剥除所有链接
- **每一次对话全量落盘**，家长面板按日期可查
- 无公开社交、无广告、无内购；数据全部存在本机

## 技术栈

Vite 7 · React 19 · TypeScript · Tailwind CSS 4 · Blockly 11（zelos 渲染器 + 自定义中文积木）· zustand · Express 5 · tsx 运行时。执行器把积木编译成带 guard 的 async JS 在页面内沙箱执行——孩子的死循环不会卡死页面，⏹ 随时可停

## 后续路线（见设计文档）

- L3 积木 ⇄ Python 双视图（向文字代码过渡）
- AI 训练场（Teachable Machine 式：孩子训练自己的识别模型）
- 语音输入、更多课程包、作品导出为独立 HTML 分享亲友
